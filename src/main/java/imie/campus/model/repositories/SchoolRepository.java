package imie.campus.model.repositories;

import imie.campus.core.repositories.EntityRepository;
import imie.campus.model.entities.School;

public interface SchoolRepository extends EntityRepository<School,Integer> {
}
