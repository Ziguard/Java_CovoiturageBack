package imie.campus.model.repositories;

import imie.campus.core.repositories.EntityRepository;
import imie.campus.model.entities.ServiceCategory;

public interface ServiceCategoryRepository extends EntityRepository<ServiceCategory,Integer> {
}
