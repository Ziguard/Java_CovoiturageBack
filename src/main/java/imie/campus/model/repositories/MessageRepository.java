package imie.campus.model.repositories;

import imie.campus.core.repositories.EntityRepository;
import imie.campus.model.entities.Message;

public interface MessageRepository extends EntityRepository<Message,Integer> {
}
