package imie.campus.model.repositories;

import imie.campus.core.repositories.EntityRepository;
import imie.campus.model.entities.Notation;

public interface NotationRepository extends EntityRepository<Notation,Integer> {
}
