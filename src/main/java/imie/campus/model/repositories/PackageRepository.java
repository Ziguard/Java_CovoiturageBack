package imie.campus.model.repositories;

import imie.campus.core.repositories.EntityRepository;
import imie.campus.model.entities.Package;

public interface PackageRepository extends EntityRepository<Package,Integer> {
}
